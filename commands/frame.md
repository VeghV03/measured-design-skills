---
description: Frame the problem — audience with a frequency, the one worst outcome, and a checkable vocabulary policy.
argument-hint: "[the brief, or a link to it]"
---
# /frame
Stage 01. Use `measured-design-frame`.

## Steps
1. Name the audience as a person with a frequency, using `design-research-user-persona` and `thinking-jobs-to-be-done`.
2. Name the one worst outcome, using `ai-alignment-reasoning-harm-anticipation` and `thinking-pre-mortem`.
3. Say what the frequency claim reverses in the agreed scope.
4. Write the vocabulary policy into `model/policy.json` with zones, not as a note.
5. Run `thinking-socratic` over the brief and list the assumptions you are about to adopt.

## Output
A problem statement, the audience, the worst outcome, and `model/policy.json`.
